name: iOS Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci --legacy-peer-deps

      - name: Create .env file
        run: |
          echo "DATABASE_URL=postgresql://placeholder" > .env
          echo "NEXTAUTH_SECRET=placeholder-secret-for-build" >> .env
          echo "NEXTAUTH_URL=https://placeholder.com" >> .env

      - name: Build Web Assets
        run: npm run build
        
      - name: Sync Capacitor
        run: npx cap sync ios

      - name: Setup Keychain (Fix for Code 65)
        run: |
          security create-keychain -p "" build.keychain
          security list-keychains -s build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security set-keychain-settings -t 3600 -l ~/Library/Keychains/build.keychain

      - name: Build for Simulator (Ad-Hoc Signing)
        run: |
          cd ios/App
          # Clean Pods
          rm -rf Pods
          rm -f Podfile.lock
          pod install --repo-update
          
          # Build with Ad-Hoc signing
          xcodebuild build \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone SE (3rd generation)' \
            -derivedDataPath ../../build \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="-" \
            SWIFT_OPTIMIZATION_LEVEL="-Onone" \
            GCC_OPTIMIZATION_LEVEL="0" \
            DISABLE_MANUAL_TARGET_ORDER_BUILD_WARNING=YES

      - name: Package Simulator App as IPA
        run: |
          echo "📂 Finding built App.app..."
          APP_PATH=$(find build -name "App.app" -type d | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ Error: App.app not found!"
            exit 1
          fi
          
          echo "✅ Found App at: $APP_PATH"
          mkdir Payload
          cp -r "$APP_PATH" Payload/
          zip -r App-Simulator.ipa Payload

      - name: Get Version
        id: version
        run: echo "version=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT

      - name: Upload IPA Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ios-ipa-v${{ steps.version.outputs.version }}
          name: MALA3EBNA iOS IPA v${{ steps.version.outputs.version }}
          body: |
            ## 🍎 MALA3EBNA - iOS IPA (Simulator)
            
            ⚠️ **تنبيه:** هذا الملف مخصص للمحاكي (Simulator) فقط.
            
            ### 📥 Download
            Download `App-Simulator.ipa` below.
          files: |
            App-Simulator.ipa
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
